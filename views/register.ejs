<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Register</title>
  </head>
  <body>
    <%- include("header.ejs") %>
    <form action="http://localhost:8000/register" method="post">
      <label for="fullname">Full Name</label>
      <input type="text" id="fullname" name="fullname" /><br />
      <label for="email">Email</label>
      <input type="email" id="email" name="email" /><br />
      <label for="phone">Phone</label>
      <input type="text" id="phone" name="phone" /><br />
      <label for="password">password</label>
      <input type="password" id="password" name="password" hashed /><br />
      <div class="button-container">
        <button type="submit">Register</button><br />
        <p>Have an account?</p>
        <a href="/login" style="text-decoration: none">Login</a>
      </div>
    </form>
  </body>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f4f4;
      padding: 10px;
      width: 100%;
      margin: auto;
      display: block;
    }
    form {
      flex-direction: column;
      gap: 10px;

      margin-top: 40px;
      width: 35%;
      margin: auto;
      background-color: white;
      padding: 10px;
      border-radius: 5px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }

    label {
      margin-left: 60px;
      display: block;
      margin-bottom: 5px;
    }

    input[type="text"],
    input[type="email"],
    input[type="password"] {
      margin-left: 60px;
      width: 60%;
      padding: 8px;
      margin-bottom: 10px;
      border-radius: 4px;
      border: 1px solid #ccc;
      align-content: center;
    }

    button {
      background-color: #df4578;
      color: white;
      padding: 10px 60px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      margin-left: 60px;
    }

    button:hover {
      background-color: rgb(228, 2, 40);
    }
    .button-container {
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    form {
      margin-top: 20px;
    }
    @media (max-width: 700px) {
      form {
        width: 90%;
        padding: 15px;
      }
      label,
      input[type="text"],
      input[type="email"],
      input[type="password"],
      button {
        margin-left: 0;
        width: 100%;
      }
      .button-container {
        flex-direction: column;
        gap: 10px;
        align-items: stretch;
      }
    }
  </style>
  <%- include("footer") %>
</html>
