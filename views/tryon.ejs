<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Virtual Try-on Hairstyles</title>
    <style>
      body {
        margin: 0;
        font-family: Arial, sans-serif;
        background-color: #f9f9f9;
      }

      h3 {
        text-align: center;
        margin-top: 20px;
        font-size: 24px;
      }

      .container {
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
        gap: 20px;
        margin: 30px auto;
        max-width: 1200px;
        padding: 0 20px;
      }

      .box {
        border-radius: 20px;
        padding: 30px;
        width: 45%;
        min-width: 300px;
        height: auto;
        box-sizing: border-box;
      }

      .image-upload {
        background-color: #ffc0cb; /* pink */
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
      }

      .image-upload label {
        font-size: 18px;
        margin-bottom: 10px;
        color: #555;
      }

      .display {
        background-color: #add8e6; /* light blue */
      }

      form {
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 10px;
      }

      input[type="file"] {
        margin-top: 10px;
      }

      button {
        padding: 10px 20px;
        border: none;
        background-color: #555;
        color: white;
        border-radius: 5px;
        cursor: pointer;
      }

      button:hover {
        background-color: #333;
      }

      @media (max-width: 768px) {
        .box {
          width: 100%;
        }
      }
    </style>
  </head>
  <body>
    <h3>Use AI to Generate Your Desired Hairstyles</h3>

    <div class="container">
      <!-- Upload Box -->
      <div class="box image-upload">
        <label for="image">Upload your image here:</label>
        <form action="/tryon" method="POST" enctype="multipart/form-data">
          <input
            type="file"
            id="image"
            name="image"
            accept="image/*"
            required
          />
          <button type="submit">Upload</button>
          <button type="submit" name="generate">Generate</button>
        </form>
      </div>

      <!-- Display Box -->
      <div class="box display">
        <p style="text-align: center; color: #333">
          Generated hairstyle will appear here
        </p>
      </div>
    </div>
  </body>
</html>
